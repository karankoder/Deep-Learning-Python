import sys

# Retrieve the list of built-in modules
built_in_modules = sys.builtin_module_names

# Print the available built-in modules
for module in built_in_modules:
    print(module)
print(built_in_modules)

